# n8n workflows

Exported workflow JSON goes here (Settings → Download in the n8n UI, or the
n8n CLI `n8n export:workflow`). Nothing exported yet — workflows get built
directly against the API service endpoints starting in Phase 1 (see
docs/ARCHITECTURE.md §12).
